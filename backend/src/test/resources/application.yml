spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mysawit
    username: mysawit
    password: penyakitsawitgila
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: dummy-id
            client-secret: dummy-secret
            scope: email, profile

supabase:
  url: http://localhost-dummy
  key: dummy-key-untuk-test
  bucket: images