spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/mysawit}
    username: ${DB_USERNAME:mysawit}
    password: ${DB_PASSWORD:penyakitsawitgila}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: dummy-id
            client-secret: dummy-secret
            scope: email, profile

supabase:
  url: ${SUPABASE_URL:http://localhost-dummy}
  key: ${SUPABASE_KEY:dummy-key-untuk-test}
  bucket: ${SUPABASE_BUCKET:images}